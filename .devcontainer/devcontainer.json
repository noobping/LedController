{
    "build": {
        "dockerfile": "Dockerfile"
    },
    "settings": {},
    "extensions": [
        "ms-python.python",
        "mhutchie.git-graph",
        "Tyriar.sort-lines"
    ],
    "postCreateCommand": "pip install --no-cache-dir -r requirements.txt opencv-python numpy"
}